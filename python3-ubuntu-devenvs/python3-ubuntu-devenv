# Green Unicorn

# System Packages 
sudo apt-get install -y build-essential libssl-dev libffi-dev python-dev
sudo apt-get install -y python3-dev python3-pip python3-virtualenv python3-venv
# Create a new directory to house your virtual envs;
# cd ~
# mkdir -p ~/venvs

# Specify python3 for the installation and then activate my virtual env
# python3 -m venv venv/flaskproject
# source ~/venvs/flaskproject/bin/activate

alias python=python3

# Update and install wheel
pip install --upgrade && pip install wheel
pip install flask gunicorn

# source sample_venv/bin/activate
# deactivate



sudo apt update
sudo apt install -y git
git --version

git config --global user.name "Sungwon Chung"
git config --global user.email "sungpia@me.com"
# ~/.gitconfig

sudo apt install -y vim
git config --global core.editor "vim"
